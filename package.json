{
  "name": "elimination-week-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch src --exec 'npm run build && node dist/index.js'",
    "watch": "babel src --watch -d dist",
    "build": "babel src -d dist",
    "dev": "npm run build && node dist/index.js",
    "update": "npm run build && pm2 restart index"
  },
  "author": "Yesh Chandiramani and Will Mundy",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.17.1",
    "express": "^4.15.2",
    "lodash": "^4.17.4",
    "mongoose": "^4.9.1",
    "mongoose-api-query": "^0.1.1-pre",
    "mongoose-createdmodified": "0.0.2"
  },
  "devDependencies": {
    "babel-core": "^6.24.0",
    "babel-loader": "^6.4.1",
    "babel-preset-es2015": "^6.24.0"
  }
}
